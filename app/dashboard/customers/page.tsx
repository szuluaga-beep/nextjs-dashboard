import React from 'react'

import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'customers',
};

const page = () => {
    return (
        <div>Customer page</div>
    )
}

export default page